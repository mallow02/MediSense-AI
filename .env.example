# Get a free API key at https://console.anthropic.com/settings/keys
# Copy this file to ".env" and paste your real key below.
# Without this key, MediSense AI still works fully using its
# built-in offline rule-based engine (Rural Health Mode).

ANTHROPIC_API_KEY=your_api_key_here
